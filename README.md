# Secret-Key-Scanner
Secret-Key-Scanner is a fast, lightweight, static security analysis tool written in Python. It scans local project directories for hardcoded API keys, passwords, database connection credentials, tokens, and private keys before code gets committed or pushed. Designed for developers and DevSecOps teams to prevent sensitive data leaks at the source.
